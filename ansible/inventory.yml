all:
  children:
    k8s_cluster:
      children:
        k8s_master:
          hosts:
            master:
              ansible_host: 
        k8s_workers:
          hosts:
            worker1:
              ansible_host: 
            worker2:
              ansible_host: 

    # sonarqube:
    #   hosts:
    #     sonarqube:
    #       ansible_host: <SONARQUBE_PUBLIC_IP>
    # nexus:
    #   hosts:
    #     nexus:
    #       ansible_host: <NEXUS_PUBLIC_IP>
    # jenkins:
    #   hosts:
    #     jenkins:
    #       ansible_host: <JENKINS_PUBLIC_IP>
